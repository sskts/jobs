/**
 * テストサンプル
 *
 * @ignore
 */

import * as assert from 'assert';
// import * as mongoose from 'mongoose';

// before(async () => {
// });

describe('sample test', () => {
    it('ok', () => {
        assert(true);
    });
});
