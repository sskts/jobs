{
  "author": {
    "name": "Motionpicture co.,ltd."
  },
  "contributors": [
    {
      "name": "Tetsu Yamazaki",
      "email": "yamazaki@motionpicture.jp"
    }
  ],
  "dependencies": {
    "@motionpicture/coa-service": "^3.0.1",
    "@motionpicture/sskts-domain": "^13.0.3",
    "debug": "^2.6.8",
    "moment": "^2.18.1"
  },
  "description": "This is a repogitory for SSKTS jobs.",
  "devDependencies": {
    "@types/debug": "0.0.29",
    "@types/mocha": "^2.2.41",
    "ink-docstrap": "^1.3.0",
    "jsdoc": "^3.4.3",
    "mocha": "^3.4.1",
    "nsp": "^2.6.3",
    "rimraf": "^2.6.1",
    "snyk": "^1.30.1",
    "tslint": "^4.5.1",
    "tslint-microsoft-contrib": "^4.0.1",
    "typescript": "^2.3.2"
  },
  "engines": {
    "node": ">=6.9.1",
    "npm": ">=3.10.8"
  },
  "keywords": [],
  "license": "UNLICENSED",
  "name": "sskts-jobs",
  "private": true,
  "repository": {},
  "scripts": {
    "build": "tsc",
    "check": "tslint --type-check --project tsconfig.json -c tslint.json bin/**/*.ts && nsp check && snyk auth && snyk test --ignore-policy",
    "clean": "rimraf bin/**/*.js test/**/*.js docs/**/* npm-debug.log*",
    "jsdoc": "node_modules/.bin/jsdoc -c jsdoc.json",
    "test": "npm run check && mocha --no-timeouts --recursive",
    "preversion": "npm run clean && npm run build && npm test",
    "version": "git add -A",
    "postversion": "git push origin"
  },
  "version": "2.0.20"
}
